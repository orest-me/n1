{
  "name": "n1.community",
  "private": true,
  "scripts": {
    "build": "node build.js && npx @tailwindcss/cli -i input.css -o dist/static/style.css --minify",
    "dev": "trap 'kill 0' EXIT; node --watch-path=./content --watch-path=./build.js build.js & npx @tailwindcss/cli -i input.css -o dist/static/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.2.0",
    "markdown-it-container": "^4.0.0",
    "typograf": "^7.6.0"
  }
}
