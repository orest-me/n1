{
  "name": "n1.community",
  "private": true,
  "scripts": {
    "build": "node build.js && npx @tailwindcss/cli -i input.css -o dist/static/style.css --minify",
    "dev": "trap 'kill 0' EXIT; node --watch build.js & npx @tailwindcss/cli -i input.css -o dist/static/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4"
  },
  "dependencies": {
    "typograf": "^7.6.0"
  }
}
